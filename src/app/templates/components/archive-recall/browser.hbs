{{#promise-proxy-container proxy=browserRequiredDataProxy}}
  {{file-browser
    dir=currentBrowsableItem
    dirError=currentBrowsableItemError
    browserModel=browserModel
    parentModalDialogSelector=parentModalDialogSelector
    showSelectionToolkit=false
    isSpaceOwned=space.currentUserIsOwner
    space=space
    contentScroll=contentScroll
    selectedItems=selectedItems
    ignoreDeselectSelector=ignoreDeselectSelector
    changeSelectedItems=(action "changeSelectedItems")
    updateDirEntityId=(action "changeDirId")
  }}
{{/promise-proxy-container}}

{{filesystem-select-browser-extension
  selectorModel=this
  browserModel=browserModel
}}

{{yield}}