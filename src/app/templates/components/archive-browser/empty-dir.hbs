<h1 class="empty-dir-text dir-box-text">
  {{tt this "noArchives"}}
</h1>
{{#if (and canCreateArchive (not datasetIsDetached))}}
  <h2 class="empty-dir-text dir-box-text">
    <a class="empty-archives-create-action" {{action "openCreateArchiveModal"}}>
      <div class="inline-icon-container bind-right-text">
        {{one-icon icon="browser-archive-add"}}
      </div>&nbsp;{{tt this "createArchive"}}
    </a>
  </h2>
{{/if}}