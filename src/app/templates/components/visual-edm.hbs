<ul class="visual-edm-object-list">
  {{#each edmMetadata.edmObjects as |edmObject|}}
    <VisualEdm::Object @model={{edmObject}} />
  {{/each}}
</ul>
{{#if edmMetadata.hasExtraData}}
  {{!-- FIXME: ładny komunikat z ikoną (i) --}}
  {{tt this "hasExtraData"}}
{{/if}}