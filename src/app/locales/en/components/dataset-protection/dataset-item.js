import { headerData, headerMetadata } from './-common';

export default {
  toggleLabels: {
    data: headerData,
    metadata: headerMetadata,
  },
  changingWriteProtectionSettings: 'changing write protection settings',
};
