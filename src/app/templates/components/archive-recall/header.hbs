<h1>
  <span class="header-text">
    {{!-- FIXME: shorter text on tablet/mobile? --}}
    {{tt this "headerText"}}
  </span>
</h1>
{{modal-archive-subheader archives=(array archive)}}
<p class="intro-text">
  {{!-- TODO: VFS-8320 make archive size text and util and fix in other places --}}
  {{tt this "intro"
    filesCount=filesCount
    filesText=(tt this (concat "filesText." (if (eq filesCount 1) "singular" "plural")))
    size=sizeText
  }}
</p>
{{yield}}