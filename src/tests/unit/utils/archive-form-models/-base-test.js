import { expect } from 'chai';
import { describe, it } from 'mocha';
import archiveFormModelsBase from 'oneprovider-gui/utils/archive-form-models/-base';

describe('Unit | Utility | archive form models/ base', function() {
  // Replace this with your real tests.
  it('works', function() {
    let result = archiveFormModelsBase();
    expect(result).to.be.ok;
  });
});
