import { expect } from 'chai';
import { describe, it } from 'mocha';
import filePropertiesRequirementQuery from 'oneprovider-gui/utils/file-properties-requirement-query';

describe('Unit | Utility | file-properties-requirement-query', function() {
  // Replace this with your real tests.
  it('works', function() {
    let result = filePropertiesRequirementQuery();
    expect(result).to.be.ok;
  });
});
