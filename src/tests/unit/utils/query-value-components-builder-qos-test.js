import { expect } from 'chai';
import { describe, it } from 'mocha';
import queryValueComponentsBuilderQos from 'oneprovider-gui/utils/query-value-components-builder-qos';

describe('Unit | Utility | query value components builder qos', function() {
  // Replace this with your real tests.
  it('works', function() {
    let result = queryValueComponentsBuilderQos();
    expect(result).to.be.ok;
  });
});
