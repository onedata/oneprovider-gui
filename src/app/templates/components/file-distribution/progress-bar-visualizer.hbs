<div class="progress-bar-container">
  <div class="progress-bar-scale-container"></div>
  {{#if isDataIncomplete}}
    <div class="progress-bar-text na-text error-text">
      <TruncatedString>
        {{#if (eq errorOnStorage.id "dirStatsDisabledForSpace")}}
          <a
            href={{providersUrl}}
            class="dir-statistics-settings-url navy"
            target={{navigateProvidersTarget}}
          >
            {{tt this "disableStatistics"}}
            <OneIcon @icon="overview" class="inline-icon-container dir-stats-icon" />
            <OneTooltip @title={{tt this "naTip"}} />
          </a>
        {{else if errorOnStorage}}
          {{tt this "error"}}
          <OneIcon
            @icon="sign-warning-rounded"
            class="inline-icon-container error-icon"
          />
          <OneTooltip @title={{errorMessage}} />
        {{/if}}
      </TruncatedString>
    </div>
    <div class="progress-bar-background"></div>
  {{else}}
    <div class="progress-bar bar" style={{occupiedSpaceBarStyle}}>
      <div class="size-label">
        {{bytes-to-string size}}
      </div>
    </div>
  {{/if}}
</div>
<div class="percentage-text">
  {{#if isDataIncomplete}}
    <span>{{tt this "na"}}</span>
  {{else}}
    {{#if (and (lt percentage 1) (not (eq percentage 0)))}}&lt;{{/if}}{{percentageText}}
  {{/if}}
</div>
{{yield}}