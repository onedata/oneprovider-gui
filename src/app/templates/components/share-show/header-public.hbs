<div class="onedata-share-head">
  <div class="share-logo hidden-custom-xs">
    <OneIcon @icon="globe-filled" />
    {{#if share.hasHandle}}
      <div class="public-data-logo-badge">{{tt this "publicData"}}</div>
    {{/if}}
  </div>
  <div class="share-name-section">
    <TruncatedString class="share-name">
      {{share.name}}
    </TruncatedString>
    <ShareShow::PublicUrlViewer
      class="row share-header-row share-header-public-url share-header-public-url-mobile"
      @compact={{true}}
      @share={{share}}
      @showHandle={{showHandle}}
      @selectedUrlType={{selectedUrlType}}
      @changeSelectedUrlType={{action (mut selectedUrlType)}}
    />
  </div>
</div>
