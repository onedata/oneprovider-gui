{{#if record.transferProgressError}}
  <ErrorInline @error={{record.transferProgressError}} />
{{else}}
  <span class="replicated-files">{{replicatedFilesStringified}}</span>
  <br />
  <small class="text-muted replicated-bytes">{{bytes-to-string replicatedBytes}}</small>
{{/if}}
