{{#if text}}
  <strong>{{text}}{{if (or errorMessages.length customMessage) ":" "."}}</strong>
{{/if}}
{{#if customMessage}}
  {{customMessage}}.
{{else}}
  {{#each errorMessages as |message index|}}
    <span>{{message}}</span>{{if (eq index (sub errorMessages.length 1)) "." ";"}}
  {{/each}}
{{/if}}