{{#one-modal
  class="datasets-modal"
  open=open
  size="xl"
  backdropClose=false
  keyboard=false
  onHide=(action "onHide")
  as |modal|
}}
  {{file-datasets
    modal=modal
    files=files
    close=(action "onHide")
  }}
{{/one-modal}}
