{{!-- FIXME: refactor to angle comopnents --}}
<div class="icon-section">
  {{one-icon icon=objectIcon}}
</div>
<div class="content-section">
  <div class="basic-info-row">
    <div class="basic-info-col">
      <div class="row modern-iconified-block-row title-row">
        <div class="edm-object-type item-name">{{objectTypeName}}</div>
        <div class="edm-xml-tag">({{model.xmlTagName}})</div>
      </div>
    </div>
  </div>
  <ul class="edm-object-attrs-container edm-object-list-container edm-attrs-container">
    {{!-- FIXME: maybe to reuse in object --}}
    {{#each attrItems as |attrItem|}}
      <li class="edm-attr-container">
        <div class="edm-attr-name"><span class="name-value">{{attrItem.name}}</span>:</div>
        <div class="edm-attr-value">{{attrItem.value}}</div>
      </li>
    {{/each}}
  </ul>
  <div class="edm-object-properties-container edm-object-list-container main-content-row row modern-iconified-block-row">
    {{#each edmProperties as |edmProperty|}}
      <VisualEdm::Property @model={{edmProperty}} />
    {{/each}}
  </div>
  {{#if model.hasExtraData}}
    <VisualEdm::ExtraDataInfo @model={{model}}/>
  {{/if}}
</div>
{{yield}}