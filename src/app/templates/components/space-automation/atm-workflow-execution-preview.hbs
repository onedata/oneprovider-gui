{{#promise-proxy-container proxy=initialLoadingProxy}}
  {{actions-toolbar
    isMinimized=false
    dropdownPlacement="auto"
    toolbarClasses="workflow-actions"
    actionsArray=workflowActions
  }}
  {{workflow-visualiser
    mode="view"
    rawData=rawSchema
    executionDataFetcher=executionDataFetcher
    cancelExecutionAction=cancelAction
  }}
{{/promise-proxy-container}}
{{yield}}
