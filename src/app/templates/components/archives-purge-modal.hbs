{{#one-modal
  open=open
  backdropClose=(not processing)
  keyboard=(not processing)
  onHide=(action "hide")
  class="proceed-modal archives-purge-modal"
  as |modal|
}}
  {{archives-purge
    archives=archives
    modal=modal
    onClose=(action "hide")
  }}
{{/one-modal}}