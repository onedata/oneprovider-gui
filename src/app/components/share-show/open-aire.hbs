<ShareShow::XmlOnlyMetadata
  @locale={{this.locale}}
  @onGenerateInitialXml={{this.generateInitialXml}}
  @xmlValue={{@xmlValue}}
  @handleService={{@handleService}}
  @space={{@space}}
  @isReadOnly={{@isReadOnly}}
  @isPublished={{@isPublished}}
  @isPublicView={{@isPublicView}}
  @initialData={{@initialData}}
  @onModify={{@onModify}}
  @onUpdateXml={{@onUpdateXml}}
  @onSubmit={{@onSubmit}}
  @onBack={{@onBack}}
  @onChangeEditMode={{@onChangeEditMode}}
  class="share-show-open-aire"
  ...attributes
/>