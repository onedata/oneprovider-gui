{{file-info-modal
  open=files
  files=files
  space=space
  share=share
  browserModel=browserModel
  previewMode=previewMode
  getDataUrl=(action "getDataUrl")
  onHidden=(action "closeInfoModal")
}}
