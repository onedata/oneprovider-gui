<span class="storage-option-storage-line">
  {{#if onlyId}}
    #<span class="model-id">{{itemValue}}</span>
  {{else}}
    <NameConflict
      @item={{hash name=itemValue.name conflictLabel=itemValue.provider.name}}
      @separator=" @"
    />
  {{/if}}
</span>