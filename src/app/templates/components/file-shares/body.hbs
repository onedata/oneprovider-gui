{{#promise-proxy-container proxy=sharesProxy}}
  {{#each shares as |share|}}
    {{file-browser/fb-share-modal-item
      share=share
      getShareUrl=(action "getShareUrl")
    }}
  {{/each}}
{{/promise-proxy-container}}