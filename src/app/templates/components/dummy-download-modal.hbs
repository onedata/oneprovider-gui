<PromiseProxyContainer @proxy={{fileProxy}}>
  {{#if opened}}
    <FileBrowser::FbDownloadModal
      @file={{fileProxy.content}}
      @onHide={{action "closeModal"}}
      @confirmDownload={{action "download"}}
    />
  {{/if}}
</PromiseProxyContainer>
