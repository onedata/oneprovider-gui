<LoadingContainer
  @isLoading={{record.userProxy.isPending}}
  @centered={{false}}
  @sizeClass="xs"
>
  <FilesystemBrowser::TableCellOwnerInfo
    @owner={{record.userProxy.content}}
    @errorReason={{record.userProxy.reason}}
  />
</LoadingContainer>