{{#if record.transferProgressError}}
  <ErrorInline @error={{record.transferProgressError}} />
{{else}}
  <OneIcon @icon={{_icon}} class="transfer-status-icon" />
  <BsTooltip @placement="top" @title={{_hint}} />
{{/if}}
