<div class="header-text-container">
  <h1 class="dataset-protection-modal-header">
    <span class="header-text">
      {{tt this "protectionHeader"}}
    </span>
    <div class="inline-icon-container">
      <OneIcon
        @icon="sign-question-rounded"
        class="hint-trigger protection-header-hint-trigger clickable"
      />
    </div>
    <OneSwitchablePopoverModal
      class="protection-hint"
      @modalClass="modal-compact-help"
      @triggersConfiguration=".protection-header-hint-trigger"
      @layoutType="modal"
      @popoverStyle="protection-hint-info"
      as |ospm|
    >
      <ospm.modal.header>
        <h1>{{tt this "hint.title"}}</h1>
      </ospm.modal.header>
      <ospm.modal.body>
        <p>{{tt this "hint.intro"}}</p>
      </ospm.modal.body>
      <ospm.modal.footer>
        <div class="btn-toolbar text-right visible-xs-block">
          <OneButton class="btn btn-sm" @type="info" @onClick={{ospm.close}}>
            {{tt this "hint.close"}}
          </OneButton>
        </div>
      </ospm.modal.footer>
    </OneSwitchablePopoverModal>
  </h1>
  <ModalDatasetSubheader @datasets={{array dataset}} />
</div>

<DatasetProtection::EffectiveInfo @file={{file}} @mode="dataset" />
