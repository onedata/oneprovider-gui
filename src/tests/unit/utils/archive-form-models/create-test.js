import { expect } from 'chai';
import { describe, it } from 'mocha';
import archiveFormModelsCreate from 'oneprovider-gui/utils/archive-form-models/create';

describe('Unit | Utility | archive form models/create', function() {
  // Replace this with your real tests.
  it('works', function() {
    let result = archiveFormModelsCreate();
    expect(result).to.be.ok;
  });
});
